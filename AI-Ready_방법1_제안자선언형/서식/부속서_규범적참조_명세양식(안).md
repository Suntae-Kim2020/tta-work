# [부속서 양식(안)] 기계가독 산출물 — 규범적 참조 명세

작성지침 6.6 나)(참조형)에 따라 표준초안의 부속서로 작성한다.
산출물 분량이 많아 문서에 수록하기 곤란한 경우에만 사용하며, 수록 가능하면 내장형(6.6 가)이 원칙이다.

---

## 부속서 A 기계가독 산출물

**(본 부속서는 표준 내용의 일부임)**

### A.1 규범성

이 부속서가 지정하는 기계가독 산출물은 본 표준의 규범 내용이다. 산출물에 적합하지 아니한 구현은 본 표준에 적합하다고 할 수 없다.

### A.2 산출물 명세

| 항목 | 기재 |
|---|---|
| 산출물 이름 | |
| 형식 | JSON Schema / XML Schema / Turtle / JSON-LD @context 등 |
| 판·버전 | |
| 무결성 확인 값 | `sha256:` |
| 이용조건 | |

### A.3 소재

| 구분 | 위치 |
|---|---|
| **협회 보관본 (정본)** | |
| 외부 공개본 | |

> 보관본과 외부 공개본의 내용이 다른 경우에는 **보관본에 따른다**(「정보통신표준화 운영요령」 제19조의3제4항).

### A.4 구성 파일

산출물이 여러 파일로 이루어진 경우 모두 적는다. 다른 파일을 `include`·`import`하는 **진입점 파일을 명시**한다.

| No. | 파일명 | 역할 | 무결성 확인 값 |
|---|---|---|---|
| 1 | | **진입점** | |
| 2 | | 모듈 | |

> **진입점 표시는 실무상 필수다.** 국제 사례 조사에서 OGC `gml.xsd`와 ISO `mdb.xsd`가 다른 모듈을 include하는 진입점일 뿐이어서, 그 파일만 내려받으면 동작하지 않는 문제가 실제로 확인됐다.

### A.5 일괄 내려받기

구성 파일이 여럿인 경우 개별 파일 주소와 함께 일괄 내려받기 경로를 제공한다.

| 항목 | 위치 |
|---|---|
| 압축본 | |

### A.6 인용 어휘

이 산출물이 규범적으로 사용하는 외부 어휘. 본문의 인용 표준과 일치하여야 한다.

| 어휘명 | 네임스페이스 IRI | 이용조건 |
|---|---|---|
| | | |

---

## 작성 시 유의사항

**하나.** 버전과 무결성 확인 값을 반드시 적는다. 외부 저장소는 변경될 수 있으므로, 이 두 값이 "표준이 지정한 그 산출물"을 특정한다.

**둘.** 파일명에 버전을 넣는 규칙을 권장한다(예: `mods-3-8.xsd`). 파일명이 바뀌면 문서의 링크가 깨진다. 국제 사례에서 HL7 FHIR의 `definitions.shex.zip`이 `fhir.schema.shex.zip`으로 바뀌어 기존 문서의 링크가 무효가 된 사례가 확인됐다.

**셋.** 이용조건은 이 명세뿐 아니라 **산출물 파일 자체에도** 표시한다(작성지침 6.6).
